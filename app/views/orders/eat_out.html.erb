<%= simple_form_for @order, url: create_eat_out_path do |f| %>
  	<%= f.input :zipcode, placeholder: "What is your zipcode?" %>
  	<%= f.input :radius, placeholder: "Within what radius?(25 Miles Max)" %>
    <%= f.submit "Let's see what happens"  %>
<% end %>